import {
  ColorPalettes,
  ColorFoundations,
} from '../../components/theming-values/colors/colors';
import Layout from '../../components/layout';

export default Layout;

# Overlays

## Overview

Overlays are used for styling elements in your UI. They can be decorative but often have functional uses like hover states on images or backgrounds when a component sits on raised surface e.g. a modal.

<ColorFoundations isOverlay />