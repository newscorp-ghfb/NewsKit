import Layout from '../../components/layout';
import Playground from '../../components/playground';
import {Prop} from '../../components/prop';
import {TextBoxSwatch} from '../../components/theming-values/colors';
import {LegacyBlock} from '../../components/legacy-block';
import {Flag, FlagSize} from 'newskit';

export default Layout;

# Flag

## Overview

The flag element is used as a way of showing the status of the content.

### Usage

A flag is used to show that a stream is live, a content has been created or updated.

### Theming

The default flag component uses the following style tokens, which can be overridden:

## Props

Flags have a number of props to facilitate a variety of uses:

<LegacyBlock $marginTop="sizing040">
  <Prop name="children" type="string" required>
    The content of the flag is passed as the child of the component.
  </Prop>
  <Prop name="$size" type="FlagSize" enum={FlagSize} default="Medium">
    Optional size of the Flag.
  </Prop>
  <Prop name="$stylePreset" type="string" default="interactive070">
    If provided, this overrides the style preset applied to the flag.
  </Prop>
</LegacyBlock>

## Playground

<Playground
  componentName="Flag"
  newskitPath="/flag"
  component={Flag}
  knobs={[
    {
      name: 'Content',
      propName: 'children',
      value: 'Flag Content',
    },
    {
      name: 'Flag Size',
      propName: '$size',
      options: [
        {
          label: 'Small',
          value: FlagSize.Small,
          isDefault: true
        },
        {
          label: 'Large',
          value: FlagSize.Large,
        },
      ],
    },
    {
      name: 'Style Preset',
      propName: '$stylePreset',
      options: [
        {
          label: 'interactive010',
          value: 'interactive010',
          isDefault: true
        },
        {
          label: 'interactive010Inverse',
          value: 'interactive010Inverse',
        },
        {
          label: 'interactive020',
          value: 'interactive020',
        },
        {
          label: 'interactive030',
          value: 'interactive030',
        },
        {
          label: 'interactive040',
          value: 'interactive040',
        },
      ],
    },
  ]}
/>

## Rationale

### Styling

Flags are designed to be compact and discreet as they are intended to supplement the primary content.


### Accessibility

#### Colour

Text and background colours must have a 3:1 minimum contrast ratio.
Hover states should have clear contrast change or should include a non colour designator.

