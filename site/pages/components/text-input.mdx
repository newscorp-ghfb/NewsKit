import Layout from '../../components/layout';
import Playground from '../../components/playground';
import {Prop} from '../../components/prop';
import {TextBoxSwatch} from '../../components/theming-values/colors';
import {LegacyBlock} from '../../components/legacy-block';
import {CodeFromDefaultPresets} from '../../components/code';
import {CodeFromFile} from '../../components/code';
import {TextInput} from 'newskit';

export default Layout;

# Text Input

## Overview

The Text Input component consists of a label, input and assistive text.

## Usage

Text Input allows users to enter text into a UI. They typically appear in forms and dialogs.

## Email Input Component

An Email Input component is also available.
The EmailInput component is built on top of the TextInput component and it shares the same defaults and props of TextInput.
The email input comes with a set of validation rules. The validation only works if the email input component is inside a Form component.
For more information please see the Form component.

<LegacyBlock>
  <CodeFromFile path="examples/email-input/email-input.tsx" />
</LegacyBlock>

## Props

Text Input has a number of props to facilitate a variety of uses:

<LegacyBlock marginTop="sizing040">
  <Prop name="size" type="'small' | 'medium' | 'large'" default="Medium">
    Optional size of the Text Input: Small, Medium or Large.
  </Prop>
  <Prop name="label" type="string" required>
    Label text is used to inform users as to what information is requested for a
    text input. Every text input field should have a label.
  </Prop>
  <Prop name="name" type="string">
    The name indentifier of the input field. It is required when the input is
    used inside a Form component.
  </Prop>
  <Prop name="hideLabel" type="boolean" default={false}>
    A prop can be set on the Text Input component to visually hide the label.
    The label should remain readable by screen readers by setting the arial
    label attribute on the input to the value of the label text.
  </Prop>
  <Prop name="assistiveText" type="string">
    Assistive text gives context about a field’s input, such as how the input
    will be used. It will be displayed beneath the text input.
  </Prop>
  <Prop name="disabled" type="boolean" default={false}>
    If true, the Text Input will render the disabled state of the style preset.
  </Prop>
  <Prop name="readOnly" type="boolean" default={false}>
    If true, the Text Input will render the read only state of the style preset.
  </Prop>
  <Prop name="ariaLabel" type="string">
    The aria-label attribute for the text input. As stated above, if the label
    is hidden, the label text will be placed into the aria label attribute of
    the input However, this can be overridden if the consumer sets aria-label
    prop.
  </Prop>
  <Prop name="rules" type="object">
    The text input can take validation rules. The validation only works if the
    input is inside a Form component. For more information please see the Form
    component.
  </Prop>
  <Prop name="icon" type="ReactElement<NewsKitIcon>">
    This prop allows the text input component to take a leading icon, for
    example, the search icon.
  </Prop>
  <Prop name="overrides" type="object">
    If provided, overrides the respective presets for the component and provided
    elements.
    <Prop name="width" type="string">
      If provided, this sets a fixed width to the text input. This can be a
      sizing token from the theme, or any CSS length value, e.g. 100% for a
      full-width element.
    </Prop>
    <Prop name="input" type="object">
      <Prop name="stylePreset" type="MQ<string>">
        If provided, this overrides the style preset applied to the text input.
      </Prop>
      <Prop name="spaceInset" type="MQ<string>">
        If provided, this overrides the padding of the Text Input. Otherwise,
        spaceInsetSquish010 or spaceInset030 is used depending on the `size`
        prop.
      </Prop>
      <Prop name="typographyPreset" type="MQ<string>">
        If provided, this overrides the type preset applied to the text input.
        Otherwise, utilityBody020 or utilityBody030 is used depending on the
        `size` prop.
      </Prop>
      <Prop name="spaceStack" type="MQ<string>">
        This is a spacing preset token which is applied as a margin on the
        bottom of the Block. Use this to space stacking content down the page.
      </Prop>
      <Prop name="minHeight" type="string">
        If provided, this sets a minimum height to the Text Input. This can be a
        sizing token from the theme, or any CSS length value. By default,
        sizing060, sizing080 or sizing090 is used depending on the `size` prop.
      </Prop>
      <Prop name="leadingIcon" type="object">
        <Prop name="iconOffset" type="MQ<string>">
          Applies space between the leading icon and left side of the input
          field.
        </Prop>
        <Prop name="spaceInset" type="MQ<string>">
          This spacing value overrides the padding value (applied by the
          spaceInset token) on the left of the input when the leading icon is
          showing. This avoids any input text rendering behind the icon.
        </Prop>
      </Prop>
      <Prop name="validationIcon" type="object">
        <Prop name="iconOffset" type="MQ<string>">
          Applies space between the validation icon and right side of the input
          field.
        </Prop>
        <Prop name="spaceInset" type="MQ<string>">
          This spacing value overrides the padding value (applied by the
          spaceInset token) on the right of the input when the validation icon
          is showing. This avoids any input text rendering behind the icon.
        </Prop>
        <Prop name="iconSize" type="MQ<string>">
          Overrides the iconSize of the validation icon.
        </Prop>
      </Prop>
    </Prop>
    <Prop name="inputLabel" type="object">
      <Prop name="stylePreset" type="MQ<string>">
        If provided, this overrides the style preset applied to the text input
        label.
      </Prop>
      <Prop name="typographyPreset" type="MQ<string>">
        If provided, this overrides the type preset applied to the text input
        label. Otherwise, label030 or label040 is used depending on the `size`
        prop.
      </Prop>
      <Prop name="spaceInline" type="MQ<string>">
        This is a spacing preset token which is applied as a margin on the right
        of the text input label. Use this to space inline content across the
        page.
      </Prop>
      <Prop name="spaceStack" type="MQ<string>">
        This is a spacing preset token which is applied as a margin on the
        bottom of the text input label. Use this to space stacking content down
        the page.
      </Prop>
    </Prop>
    <Prop name="assistiveText" type="object">
      <Prop name="stylePreset" type="MQ<string>">
        If provided, this overrides the style preset applied to the assistive
        text.
      </Prop>
      <Prop name="typographyPreset" type="MQ<string>">
        If provided, this overrides the type preset applied to the text input.
        Otherwise, utilityLabel020 or utilityLabel030 is used depending on the
        `size` prop.
      </Prop>
      <Prop name="minHeight" type="string">
        If provided, this sets a minimum height to the assistive text container.
        This is used to prevent the layout shift in page when the assistive text
        shows and hides.
      </Prop>
    </Prop>
    Refer to the defaults below for the object structure:
    <CodeFromDefaultPresets componentName="textInput" />
  </Prop>
</LegacyBlock>

## Playground

<Playground
  componentName="TextInput"
  component={state => <TextInput label="Label" {...state} />}
  knobs={[
    {
      name: 'Size',
      propName: 'size',
      options: [
        {
          label: 'small',
          value: 'small',
        },
        {
          label: 'Default (medium)',
          value: 'medium',
          isDefault: true,
        },
        {
          label: 'large',
          value: 'large',
        },
      ],
    },
    {
      name: 'Style Preset',
      propName: 'overrides',
      options: [
        {
          label: 'Default',
          value: {
            label: {
              stylePreset: undefined,
            },
            input: {
              stylePreset: undefined,
            },
            assistiveText: {
              stylePreset: undefined,
            },
          },
          isDefault: true,
        },
      ],
    },
    {
      name: 'Typography Preset',
      propName: 'overrides',
      options: [
        {
          label: 'Default',
          value: {
            label: {
              typographyPreset: undefined,
            },
            input: {
              typographyPreset: undefined,
            },
            assistiveText: {
              typographyPreset: undefined,
            },
          },
          isDefault: true,
        },
        {
          label: 'Different presets',
          value: {
            label: {
              typographyPreset: 'label050',
            },
            assistiveText: {
              typographyPreset: 'label050',
            },
          },
        },
      ],
    },
    {
      name: 'Spacing Preset',
      propName: 'overrides',
      options: [
        {
          label: 'Default',
          value: {
            label: {
              spaceStack: undefined,
            },
            input: {
              spaceStack: undefined,
            },
          },
          isDefault: true,
        },
        {
          label: 'Different presets',
          value: {
            input: {
              spaceStack: 'space040',
            },
          },
        },
      ],
    },
    {
      name: 'Assistive Text',
      propName: 'assistiveText',
      options: [
        {
          label: 'Unset',
          value: undefined,
          isDefault: true,
        },
        {
          label: 'With Assistive Text',
          value: 'Assistive text',
        },
      ],
    },
    {
      name: 'Placeholder',
      propName: 'placeholder',
      options: [
        {
          label: 'Default',
          value: 'Placeholder',
          isDefault: true,
        },
        {
          label: 'Custom Placeholder',
          value: 'This is some text',
        },
      ],
    },
    {
      name: 'Hide Label',
      propName: 'hideLabel',
      type: 'boolean',
      value: false,
    },
    {
      name: 'Width',
      propName: 'overrides',
      options: [
        {
          label: 'Default',
          value: {
            width: undefined,
          },
          isDefault: true,
        },
        {
          label: 'Full-width',
          value: {
            width: '100%',
          },
        },
        {
          label: 'Fix-width',
          value: {
            width: '200px',
          },
        },
      ],
    },
    {
      name: 'Disabled',
      propName: 'disabled',
      type: 'boolean',
      value: false,
    },
    {
      name: 'Read-only',
      propName: 'readOnly',
      type: 'boolean',
      value: false,
    },
  ]}
/>
