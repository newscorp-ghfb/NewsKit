import Layout from '../../components/layout';
import Playground from '../../components/playground';
import {Prop} from '../../components/prop';
import {TextBoxSwatch} from '../../components/theming-values/colors';
import {LegacyBlock} from '../../components/legacy-block';
import {
  CodeFromDefaultPresets,
  Code,
} from '../../components/code';
import {CommonLogicalPropsMDX} from '../../components/component-api/common-logical-props';


import {Byline} from 'newskit';

export default Layout;

# Byline

## Overview

The byline is a small line of text which lists the authors of an article, along with their titles if provided.

### Usage

The `Byline` component requires an array of bylineData. Each bylineData object in the array must contain an author. You can optionally provide a `href` property to link to that author's page and a title for that author.

### Theming

The byline uses a typography preset `meta02` and inherits its font color from its parent, apart from author links which use the default <a href='/components/link'>Link</a> theme color tokens.

## Props

<LegacyBlock marginTop="sizing040">
  <Prop name="bylineData" type="array" required>
    An array of byline data to populate the byline authors. Byline data objects
    must contain an author string, and can contain optional href and title
    strings.
    <Code>
      {`
interface BylineData {
  author: string;
  href?: string;
  title?: string;
  location?: string;
  ariaLabel?: string;
}
      `}
    </Code>
  </Prop>
  <Prop name="overrides" type="object">
    If provided, overrides the respective presets for the component and provided
    elements.
    <Prop name="stylePreset" type="MQ<string>">
      If provided, this overrides the style preset of the component.
    </Prop>
    <Prop name="typographyPreset" type="MQ<string>">
      If provided, this overrides the typography preset of the component.
    </Prop>
    <Prop name="spaceStack" type="MQ<string>">
      If provided, this overrides the vertical space between each line when the
      bylines items wrap onto a new line.
    </Prop>
    <Prop name="link" type="object">
      <Prop name="stylePreset" type="MQ<string>">
        If provided, this overrides the style preset applied to the passed link.
      </Prop>
      <Prop name="typographyPreset" type="MQ<string>">
        If provided, this overrides the typography preset applied to the passed
        link.
      </Prop>
    </Prop>
    <Prop name="divider" type="object">
      <Prop name="stylePreset" type="MQ<string>">
        If provided, this overrides the style preset applied to the divider.
      </Prop>
      <Prop name="spaceInline" type="MQ<string>">
        If provided, this overrides the horizontal space between the divider and
        the text.
      </Prop>
    </Prop>
    <CommonLogicalPropsMDX/>
    Refer to the defaults below for the full object structure:
    <CodeFromDefaultPresets componentName="byline" />
  </Prop>
</LegacyBlock>

## Playground

<Playground
  componentName="Byline"
  component={Byline}
  knobs={[
    {
      name: 'Byline Data',
      propName: 'bylineData',
      value: [
        {
          author: 'Francis Elliott',
          href: 'https://www.thetimes.co.uk/profile/francis-elliott',
          title: 'Political Editor',
          location: 'UK',
        },
        {
          author: 'Didi Tang',
          href: 'https://www.thetimes.co.uk/profile/didi-tang',
          title: 'Beijing Correspondent',
          location: 'UK/Beijing',
        },
      ],
    },
    {
      name: 'Style Preset Overrides',
      propName: 'overrides',
      options: [
        {
          label: 'Default',
          value: {
            stylePreset: undefined,
            link: {
              stylePreset: undefined,
            },
            divider: {
              stylePreset: undefined,
            },
          },
          isDefault: true,
        },
        {
          label: 'buttonMinimalPrimary',
          value: {
            stylePreset: 'buttonMinimalPrimary',
            link: {
              stylePreset: 'buttonMinimalPrimary',
            },
            divider: {
              stylePreset: 'buttonMinimalPrimary',
            },
          },
        },
      ],
    },
    {
      name: 'Typography Preset Overrides',
      propName: 'overrides',
      options: [
        {
          label: 'Default',
          value: {
            typographyPreset: undefined,
            link: {
              typographyPreset: undefined,
            },
          },
          isDefault: true,
        },
        {
          label: 'editorialParagraph030',
          value: {
            typographyPreset: 'editorialParagraph030',
            link: {
              typographyPreset: 'editorialParagraph030',
            },
          },
        },
      ],
    },
    {
      name: 'Margin Preset Overrides',
      propName: 'overrides',
      options: [
        {
          label: 'Default',
          value: {
            spaceStack: undefined,
            divider: {
              spaceInline: undefined,
            },
          },
          isDefault: true,
        },
        {
          label: 'increaseSpacing',
          value: {
            spaceStack: 'space040',
            divider: {
              spaceInline: 'space040',
            },
          },
        },
      ],
    },
  ]}
/>

For more information on overriding component defaults, see the docs <a href='/theme/theming/component-defaults'>here</a>.
