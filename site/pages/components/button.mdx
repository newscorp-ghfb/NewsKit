import Layout from '../../components/layout';
import Playground from '../../components/playground';
import {Prop} from '../../components/prop';
import {TextBoxSwatch} from '../../components/theming-values/colors';
import {LegacyBlock} from '../../components/legacy-block';
import {Selector} from '../../components/playground/selector';

import {Button, ButtonSize, Email, Pause} from 'newskit';

export default Layout;

# Button

## Overview

A styled button element with customisable size.

## Props

Buttons have a number of props to facilitate a variety of uses. Any prop valid on an Button HTML element as described in [here](https://developer.mozilla.org/en-US/docs/Web/HTML/Element/button#Attributes), is also valid on the Button component.
Custom props are detailed below:

<LegacyBlock marginTop="sizing040">
  <Prop
    name="$size"
    type="ButtonSize"
    enum={ButtonSize}
    default={ButtonSize.Small}
  >
    Set the size of the button. Possible values: Small, Medium and Large.
    Default styling: Small.
  </Prop>
  <Prop name="$stylePreset" type="string" default="buttonSolidPrimary">
    Set the style presets of the button.
  </Prop>
  <Prop name="icon" type="React.ComponentType">
    Defines which icon to use.
  </Prop>
  <Prop name="$iconColor" type="ColorKeys" default="buttonFill">
    Set the color of the icon.
  </Prop>
</LegacyBlock>

## Playground

<Playground
  componentName="Button"
  newskitPath="/button"
  component={state => (
    <Button {...state}>
      <Email />
      Click me!
    </Button>
  )}
  knobs={[
    {
      name: 'Size',
      propName: '$size',
      options: [
        {
          label: 'ButtonSize.Small',
          value: ButtonSize.Small,
          isDefault: true,
        },
        {
          label: 'ButtonSize.Medium',
          value: ButtonSize.Medium,
        },
        {
          label: 'ButtonSize.Large',
          value: ButtonSize.Large,
        },
      ],
    },
    {
      name: 'Style Preset',
      propName: '$stylePreset',
      options: [
        {
          label: 'buttonSolidPrimary',
          value: 'buttonSolidPrimary',
          isDefault: true,
        },
        {
          label: 'buttonOutlinedPrimary',
          value: 'buttonOutlinedPrimary',
        },
        {
          label: 'buttonMinimalPrimary',
          value: 'buttonMinimalPrimary',
        },
      ],
    },
    {
      name: 'Icon',
      propName: 'icon',
      options: [
        {
          label: 'no icon',
          value: undefined,
          isDefault: true,
        },
        {
          label: 'Email Icon',
          value: Email,
        },
        {
          label: 'Pause Icon',
          value: Pause,
        },
      ],
    },
    {
      name: 'Icon Placement',
      propName: 'iconPlacement',
      options: [
        {
          label: 'IconPlacement.Start',
          value: IconPlacement.Start,
          isDefault: true,
        },
        {
          label: 'buttonMinimalPrimary',
          value: 'buttonMinimalPrimary',
        },
      ],
    },
    {
      name: 'Disabled',
      propName: 'disabled',
      type: 'boolean',
      value: false,
    },
  ]}
/>

### Styling

The button component is block level element with a native HTML button with custom styling.

### Accessibility

The button is a fully accessible with the native HTML button used.

#### Colour

Text and background colours must have a 3:1 minimum contrast ratio.
Hover states should have clear contrast change or should include a non colour designator.
