import * as React from 'react';

import {Consent} from '..';

export const name = 'consent';

export const component = () => (
  <Consent
    sourcePointConfig={{
      accountId: '259',
    }}
  />
);
