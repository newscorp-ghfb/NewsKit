import React from 'react';
import {StackChildProps} from './types';

// Used to carry props into the Stack component
export const StackChild: React.FC<StackChildProps> = () => null;

StackChild.displayName = 'StackChild';
